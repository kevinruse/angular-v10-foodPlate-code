import { OuncesTogramsPipe } from './ounces-to-grams.pipe';

describe('OuncesTogramsPipe', () => {
  it('create an instance', () => {
    const pipe = new OuncesTogramsPipe();
    expect(pipe).toBeTruthy();
  });
});
